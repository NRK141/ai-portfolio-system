# Raw Data

This folder is for small manual input datasets that are needed by the backtester.

Examples:

- point-in-time market cap data
- manually curated universe files
- custom ticker lists

Downloaded market data, price history, cache files, and generated datasets should not be committed to GitHub.

The price cache belongs in:

```text
data/cache/